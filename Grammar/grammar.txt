P = {
    Program : Header Content '.'

    Header : PROGRAM identifier ListArgs ';'

    Content : BEGIN ListStatement END

    ListStatement : ListStatement Statement
                  | Statement

    # falta definir estes statements
    Statement : ForStatement
              | ProcedureFunctionStatement
              | VariableStatement

    ProcedureFunctionStatement: identifier '(' ListArgs ')' ';'

    # pode não receber args
    ListArgs : ListArgs ',' Arg
             | Arg

    Arg : string

    Constant : UnsignedNumber
             | Sign UnsignedNumber
             | string

    UnsignedNumber : num_int
                   | num_real

    Sign : '+'
         | '-'
}
